# Typing Speed

Typing speed web application collected in one repository.

## Projects

- `frontend/` - React and Vite typing speed website
- `backend/` - Express, MongoDB, and EJS service

Install dependencies inside each project with `npm install`. Copy the relevant
`.env.example` file to `.env` before starting a backend service.

